# 2026-05-03 迭代记录

## 搜索系统

- 新增 Cmd+K 搜索弹窗，Pagefind 全文检索，配置中文搜索界面样式
- 新增独立搜索页 `/search/`
- 修复开发模式 `public/pagefind/` 缺失导致搜索不可用（build 后自动同步）
- 修复重复打开弹窗创建多个 PagefindUI 实例的 bug，改为单例复用
- 修复搜索图标（放大镜）与输入文字重叠，改用 `background-image` 内嵌 SVG

## 性能优化

- 移除 `backdrop-filter: blur()`（`.site-header.scrolled` + `#search-modal::backdrop`），消除滚动时 GPU 持续合成卡顿
- 简化 body 背景，去掉 `radial-gradient`，只保留 `linear-gradient`
- 缩小全局 box-shadow 模糊值（80px → 32px, 46px → 16px）
- 书籍/电影封面转为 WebP 格式（400px 宽，quality 82），总计 ~1.6MB / 60 张

## Jekyll → Astro 迁移修复

- 修复 6 篇文章共 13 处 `{% post_url %}` Jekyll 标签，替换为 Astro 相对链接格式

## 封面图片系统

- 新增 `scripts/download_covers.py`，通过豆瓣 API v2 批量下载封面
- `POST /v2/book/isbn/{isbn}` 精确匹配书籍，66/70 本正确下载（3 本无 ISBN，1 本未收录）
- `POST /v2/movie/search` 按标题搜索电影，25/25 全部下载
- 自动裁切转 WebP，Referer 头绕过豆瓣 CDN 防盗链
- 已存在封面自动跳过，支持增量更新

## 草稿系统

- 新增 `src/content/drafts/` 独立内容集合，与 blog 共用 schema
- 草稿开发模式可预览，不进入首页列表、分类页、生产构建
- 目录加入 `.gitignore`

## Zotero 集成

- 新增 `scripts/convert_zotero.py`，将 Better BibTeX CSL JSON 导出转为 blog 格式
- Zotero 搜索文件夹 Auto-Export → `src/data/_zotero.json` → 自动转换
- `npm run build` 前置 `zotero:sync`，`--merge` 模式保留封面/评分/标签等手动字段
